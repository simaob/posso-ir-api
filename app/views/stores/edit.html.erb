<% content_for :page_title, I18n.t('views.edit.title', resource: Store.model_name.human) %>
<% content_for :actions do %>
  <%= link_to(I18n.t('views.actions.show'), @store,
    class: 'btn btn-sm btn-outline-secondary')
    .concat(link_to(I18n.t('views.actions.back'), stores_path,
    class: 'btn btn-sm btn-outline-secondary')) %>
<% end %>

<%= render 'form', store: @store %>
